(ns front.app.main-test
   (:require [cljs.test :refer-macros [deftest is testing run-tests]]))

;; TODO: add some tests

;; use (cljs.test/run-tests) from the Repl to run all tests
(deftest test-numbers
  (is (= 1 1)))


;; or add at the end of file 
;;(cljs.test/run-tests)